#include <iostream>
#include <string>
#include <cctype>
using namespace std;

int main() {
    string str;
    cout << "Enter a string: ";
    cin >> str;

    for(char &c : str) c = toupper(c);
    cout << "Uppercase: " << str << endl;

    return 0;
}