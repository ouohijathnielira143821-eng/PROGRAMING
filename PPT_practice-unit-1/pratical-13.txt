#include <stdio.h>
#include <string.h>

int main() {
    char str[100];int i=0;
while(str[i] != '\0') i++;
printf("Length=%d", i);
    scanf("%s", str);

    printf("Length=%lu", strlen(str));
    return 0;
}