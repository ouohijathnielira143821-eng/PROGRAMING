#include <iostream>
#include <string>
using namespace std;

int main() {
    string str1, str2;
    cout << "Enter first string: ";
    cin >> str1;
    cout << "Enter second string: ";
    cin >> str2;

    if(str1 == str2) cout << "Strings are the same.\n";
    else cout << "Strings are different.\n";

    return 0;
}